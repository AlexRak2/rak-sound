<Window x:Class="SonnissBrowser.VoiceCreatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SonnissBrowser"
        Title="Voice Creator"
        Width="750" Height="700"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Bg0}"
        WindowStyle="None"
        ResizeMode="CanResize"
        AllowsTransparency="False">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <local:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVis"/>

        <!-- Core surfaces -->
        <SolidColorBrush x:Key="Bg0" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="Bg1" Color="#252526"/>
        <SolidColorBrush x:Key="Panel" Color="#252526"/>
        <SolidColorBrush x:Key="Border" Color="#333337"/>

        <!-- Text -->
        <SolidColorBrush x:Key="Text0" Color="#CCCCCC"/>
        <SolidColorBrush x:Key="Text1" Color="#BBBBBB"/>
        <SolidColorBrush x:Key="Text2" Color="#858585"/>
        <SolidColorBrush x:Key="Text3" Color="#6A6A6A"/>

        <!-- Inputs -->
        <SolidColorBrush x:Key="InputBg" Color="#3C3C3C"/>
        <SolidColorBrush x:Key="InputBorder" Color="#555555"/>
        <SolidColorBrush x:Key="InputFg" Color="#CCCCCC"/>

        <!-- Accent -->
        <SolidColorBrush x:Key="Accent" Color="#0E639C"/>
        <SolidColorBrush x:Key="AccentBright" Color="#007ACC"/>

        <!-- Title bar -->
        <SolidColorBrush x:Key="TitleBar" Color="#111111"/>
        <SolidColorBrush x:Key="TitleBarBorder" Color="#2A2A2A"/>

        <!-- Buttons -->
        <SolidColorBrush x:Key="BtnBg" Color="#3F3F46"/>
        <SolidColorBrush x:Key="BtnBorder" Color="#555555"/>
        <SolidColorBrush x:Key="BtnFg" Color="#CCCCCC"/>
        <SolidColorBrush x:Key="BtnBgHover" Color="#505055"/>
        <SolidColorBrush x:Key="BtnBgPressed" Color="#5A5A5F"/>

        <!-- Caption buttons -->
        <SolidColorBrush x:Key="CaptionBtnFg" Color="#DDDDDD"/>
        <SolidColorBrush x:Key="CaptionBtnHoverBg" Color="#2A2A2A"/>
        <SolidColorBrush x:Key="CaptionBtnPressedBg" Color="#3A3A3A"/>
        <SolidColorBrush x:Key="CaptionBtnCloseHoverBg" Color="#C42B1C"/>
        <SolidColorBrush x:Key="CaptionBtnClosePressedBg" Color="#A52317"/>

        <!-- Button Style -->
        <Style TargetType="Button" x:Key="Btn">
            <Setter Property="Background" Value="{DynamicResource BtnBg}"/>
            <Setter Property="Foreground" Value="{DynamicResource BtnFg}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BtnBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,7"/>
            <Setter Property="MinHeight" Value="30"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                CornerRadius="3"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource BtnBgHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource BtnBgPressed}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="BtnAccent" BasedOn="{StaticResource Btn}">
            <Setter Property="Background" Value="{DynamicResource AccentBright}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AccentBright}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style TargetType="Button" x:Key="BtnSmall" BasedOn="{StaticResource Btn}">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="MinHeight" Value="24"/>
            <Setter Property="FontSize" Value="10"/>
        </Style>

        <!-- Caption Button Style -->
        <Style x:Key="CaptionBtn" TargetType="Button">
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource CaptionBtnFg}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}">
                            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource CaptionBtnHoverBg}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource CaptionBtnPressedBg}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CaptionBtnClose" TargetType="Button" BasedOn="{StaticResource CaptionBtn}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource CaptionBtnCloseHoverBg}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{DynamicResource CaptionBtnClosePressedBg}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- TextBox Style -->
        <Style TargetType="TextBox" x:Key="InputBox">
            <Setter Property="Background" Value="{DynamicResource InputBg}"/>
            <Setter Property="Foreground" Value="{DynamicResource InputFg}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalContentAlignment" Value="Top"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="2"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#6A6A6A"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBright}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Toggle Button -->
        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="20"/>
                </Grid.ColumnDefinitions>
                <Border x:Name="Border"
                        Grid.ColumnSpan="2"
                        Background="{DynamicResource InputBg}"
                        BorderBrush="{DynamicResource InputBorder}"
                        BorderThickness="1"
                        CornerRadius="3"/>
                <Border Grid.Column="0"
                        Background="Transparent"
                        Margin="1"/>
                <Path x:Name="Arrow"
                      Grid.Column="1"
                      Fill="{DynamicResource Text1}"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      Data="M 0 0 L 4 4 L 8 0 Z"/>
            </Grid>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="Border" Property="BorderBrush" Value="#6A6A6A"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <!-- ComboBox TextBox -->
        <ControlTemplate x:Key="ComboBoxTextBox" TargetType="TextBox">
            <Border x:Name="PART_ContentHost" Focusable="False" Background="Transparent"/>
        </ControlTemplate>

        <!-- Full ComboBox Style with proper dropdown colors -->
        <Style TargetType="ComboBox" x:Key="VoiceComboBox">
            <Setter Property="Background" Value="{DynamicResource InputBg}"/>
            <Setter Property="Foreground" Value="{DynamicResource Text0}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton"
                                          Template="{StaticResource ComboBoxToggleButton}"
                                          Focusable="False"
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press"/>
                            <ContentPresenter Name="ContentSite"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="10,3,23,3"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left">
                                <ContentPresenter.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{DynamicResource Text0}"/>
                                    </Style>
                                </ContentPresenter.Resources>
                            </ContentPresenter>
                            <TextBox x:Name="PART_EditableTextBox"
                                     Style="{x:Null}"
                                     Template="{StaticResource ComboBoxTextBox}"
                                     HorizontalAlignment="Left"
                                     VerticalAlignment="Center"
                                     Margin="3,3,23,3"
                                     Focusable="True"
                                     Background="Transparent"
                                     Foreground="{DynamicResource Text0}"
                                     Visibility="Hidden"
                                     IsReadOnly="{TemplateBinding IsReadOnly}"/>
                            <Popup Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid Name="DropDown"
                                      SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                            Background="{DynamicResource InputBg}"
                                            BorderBrush="{DynamicResource InputBorder}"
                                            BorderThickness="1"
                                            CornerRadius="3"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBoxItem Style with proper text color -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource Text0}"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="2">
                            <ContentPresenter>
                                <ContentPresenter.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{DynamicResource Text0}"/>
                                    </Style>
                                </ContentPresenter.Resources>
                            </ContentPresenter>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentBright}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Accent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- CheckBox Style -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource Text1}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

    </Window.Resources>

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32"
                      CornerRadius="0"
                      GlassFrameThickness="0"
                      ResizeBorderThickness="6"
                      UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Grid Background="{DynamicResource Bg0}">
        <DockPanel LastChildFill="True">

            <!-- TITLE BAR -->
            <Border DockPanel.Dock="Top"
                    Height="32"
                    Background="{DynamicResource TitleBar}"
                    BorderBrush="{DynamicResource TitleBarBorder}"
                    BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0"
                            Background="Transparent"
                            MouseLeftButtonDown="TitleBar_MouseLeftButtonDown"
                            WindowChrome.IsHitTestVisibleInChrome="True">
                        <StackPanel Orientation="Horizontal"
                                    Margin="10,0,0,0"
                                    VerticalAlignment="Center">
                            <TextBlock Text="Voice Creator"
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource Text0}"
                                       FontSize="12"
                                       FontWeight="SemiBold"/>
                            <TextBlock Text=" - Advanced TTS"
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource Text2}"
                                       FontSize="10"
                                       Margin="4,0,0,0"/>
                        </StackPanel>
                    </Border>

                    <StackPanel Grid.Column="1"
                                Orientation="Horizontal"
                                WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button Style="{StaticResource CaptionBtn}"
                                Content="—"
                                Click="Minimize_Click"
                                WindowChrome.IsHitTestVisibleInChrome="True"/>
                        <Button Style="{StaticResource CaptionBtnClose}"
                                Content="X"
                                Click="Close_Click"
                                WindowChrome.IsHitTestVisibleInChrome="True"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- STATUS BAR -->
            <Border DockPanel.Dock="Bottom"
                    Background="{DynamicResource Bg1}"
                    BorderBrush="{DynamicResource Border}"
                    BorderThickness="0,1,0,0"
                    Padding="12,8">
                <TextBlock Text="{Binding StatusText}"
                           Foreground="{DynamicResource Text2}"
                           FontSize="10"/>
            </Border>

            <!-- MAIN CONTENT -->
            <Border Background="{DynamicResource Bg1}"
                    Padding="16">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Voice Selection Section -->
                        <StackPanel Grid.Row="0" Margin="0,0,0,16">
                            <TextBlock Text="VOICE SELECTION"
                                       Foreground="{DynamicResource Text3}"
                                       FontSize="9"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,10"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="16"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Voice Selection -->
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Voice"
                                               Foreground="{DynamicResource Text2}"
                                               FontSize="10"
                                               Margin="0,0,0,4"/>
                                    <ComboBox Style="{StaticResource VoiceComboBox}"
                                              ItemsSource="{Binding AvailableVoices}"
                                              SelectedItem="{Binding SelectedVoice}">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Name}"
                                                               FontWeight="SemiBold"
                                                               Foreground="{DynamicResource Text0}"/>
                                                    <TextBlock Text="{Binding Description}"
                                                               FontSize="9"
                                                               Foreground="{DynamicResource Text2}"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>
                                    </ComboBox>
                                    <TextBlock Text="{Binding SelectedVoice.Description}"
                                               Foreground="{DynamicResource Text2}"
                                               FontSize="9"
                                               Margin="0,4,0,0"/>
                                </StackPanel>

                                <!-- Emphasis Selection -->
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Emphasis Level"
                                               Foreground="{DynamicResource Text2}"
                                               FontSize="10"
                                               Margin="0,0,0,4"/>
                                    <ComboBox Style="{StaticResource VoiceComboBox}"
                                              ItemsSource="{Binding EmphasisOptions}"
                                              SelectedItem="{Binding SelectedEmphasis}"/>
                                    <TextBlock Text="Affects how words are stressed"
                                               Foreground="{DynamicResource Text3}"
                                               FontSize="9"
                                               Margin="0,4,0,0"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>

                        <!-- Voice Parameters Section -->
                        <StackPanel Grid.Row="1" Margin="0,0,0,16">
                            <TextBlock Text="VOICE PARAMETERS"
                                       Foreground="{DynamicResource Text3}"
                                       FontSize="9"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,10"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="16"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="12"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="12"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Speed -->
                                <StackPanel Grid.Row="0" Grid.Column="0">
                                    <TextBlock Text="Speed"
                                               Foreground="{DynamicResource Text2}"
                                               FontSize="10"
                                               Margin="0,0,0,4"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="40"/>
                                        </Grid.ColumnDefinitions>
                                        <Slider Grid.Column="0"
                                                Minimum="-10" Maximum="10"
                                                Value="{Binding Rate}"
                                                TickFrequency="1"
                                                IsSnapToTickEnabled="True"
                                                VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding Rate, StringFormat={}{0:+0;-0;0}}"
                                                   Foreground="{DynamicResource Text1}"
                                                   FontSize="10"
                                                   VerticalAlignment="Center"
                                                   HorizontalAlignment="Right"/>
                                    </Grid>
                                </StackPanel>

                                <!-- Volume -->
                                <StackPanel Grid.Row="0" Grid.Column="2">
                                    <TextBlock Text="Volume"
                                               Foreground="{DynamicResource Text2}"
                                               FontSize="10"
                                               Margin="0,0,0,4"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="40"/>
                                        </Grid.ColumnDefinitions>
                                        <Slider Grid.Column="0"
                                                Minimum="0" Maximum="100"
                                                Value="{Binding Volume}"
                                                TickFrequency="5"
                                                VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding Volume, StringFormat={}{0}%}"
                                                   Foreground="{DynamicResource Text1}"
                                                   FontSize="10"
                                                   VerticalAlignment="Center"
                                                   HorizontalAlignment="Right"/>
                                    </Grid>
                                </StackPanel>

                                <!-- Pitch -->
                                <StackPanel Grid.Row="2" Grid.Column="0">
                                    <TextBlock Text="Pitch"
                                               Foreground="{DynamicResource Text2}"
                                               FontSize="10"
                                               Margin="0,0,0,4"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="40"/>
                                        </Grid.ColumnDefinitions>
                                        <Slider Grid.Column="0"
                                                Minimum="-10" Maximum="10"
                                                Value="{Binding Pitch}"
                                                TickFrequency="1"
                                                IsSnapToTickEnabled="True"
                                                VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding Pitch, StringFormat={}{0:+0;-0;0}}"
                                                   Foreground="{DynamicResource Text1}"
                                                   FontSize="10"
                                                   VerticalAlignment="Center"
                                                   HorizontalAlignment="Right"/>
                                    </Grid>
                                </StackPanel>

                                <!-- Pause Multiplier -->
                                <StackPanel Grid.Row="2" Grid.Column="2">
                                    <TextBlock Text="Natural Pauses"
                                               Foreground="{DynamicResource Text2}"
                                               FontSize="10"
                                               Margin="0,0,0,4"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="40"/>
                                        </Grid.ColumnDefinitions>
                                        <Slider Grid.Column="0"
                                                Minimum="0.5" Maximum="3"
                                                Value="{Binding PauseMultiplier}"
                                                TickFrequency="0.1"
                                                VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding PauseMultiplier, StringFormat={}{0:0.0}x}"
                                                   Foreground="{DynamicResource Text1}"
                                                   FontSize="10"
                                                   VerticalAlignment="Center"
                                                   HorizontalAlignment="Right"/>
                                    </Grid>
                                </StackPanel>

                                <!-- Enhanced Prosody Checkbox -->
                                <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal">
                                    <CheckBox Content="Enable Enhanced Prosody (SSML)"
                                              IsChecked="{Binding UseEnhancedProsody}"
                                              VerticalAlignment="Center"/>
                                    <TextBlock Text="Uses pitch, emphasis, and natural pauses"
                                               Foreground="{DynamicResource Text3}"
                                               FontSize="9"
                                               VerticalAlignment="Center"
                                               Margin="8,0,0,0"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>

                        <!-- Text Input Section -->
                        <DockPanel Grid.Row="2" Margin="0,0,0,8">
                            <Grid DockPanel.Dock="Top" Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="TEXT INPUT"
                                           Foreground="{DynamicResource Text3}"
                                           FontSize="9"
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center"/>

                                <StackPanel Grid.Column="2" Orientation="Horizontal">
                                    <CheckBox Content="SSML Mode"
                                              IsChecked="{Binding UseSsmlMode}"
                                              VerticalAlignment="Center"
                                              Margin="0,0,12,0"/>
                                    <Button Style="{StaticResource BtnSmall}"
                                            Content="Generate SSML"
                                            Command="{Binding GenerateSsmlCommand}"
                                            ToolTip="Convert text to editable SSML template"/>
                                </StackPanel>
                            </Grid>

                            <!-- Normal Text Input -->
                            <TextBox Style="{StaticResource InputBox}"
                                     Text="{Binding TextInput, UpdateSourceTrigger=PropertyChanged}"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     VerticalScrollBarVisibility="Auto"
                                     MinHeight="100"
                                     Visibility="{Binding UseSsmlMode, Converter={StaticResource InverseBoolToVis}}"/>

                            <!-- SSML Input -->
                            <DockPanel Visibility="{Binding UseSsmlMode, Converter={StaticResource BoolToVis}}">
                                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,6">
                                    <Button Style="{StaticResource BtnSmall}"
                                            Content="+ Pause"
                                            Command="{Binding InsertPauseCommand}"
                                            Margin="0,0,6,0"/>
                                    <Button Style="{StaticResource BtnSmall}"
                                            Content="+ Emphasis"
                                            Command="{Binding InsertEmphasisCommand}"/>
                                    <TextBlock Text="Edit the SSML below for advanced control"
                                               Foreground="{DynamicResource Text3}"
                                               FontSize="9"
                                               VerticalAlignment="Center"
                                               Margin="12,0,0,0"/>
                                </StackPanel>
                                <TextBox Style="{StaticResource InputBox}"
                                         Text="{Binding SsmlInput, UpdateSourceTrigger=PropertyChanged}"
                                         AcceptsReturn="True"
                                         TextWrapping="Wrap"
                                         VerticalScrollBarVisibility="Auto"
                                         FontFamily="Consolas"
                                         FontSize="11"
                                         MinHeight="100"/>
                            </DockPanel>
                        </DockPanel>

                        <!-- Action Buttons -->
                        <StackPanel Grid.Row="3"
                                    Orientation="Horizontal"
                                    HorizontalAlignment="Right"
                                    Margin="0,8,0,0">
                            <Button Style="{StaticResource Btn}"
                                    Content="Stop"
                                    Command="{Binding StopCommand}"
                                    Width="80"
                                    Margin="0,0,8,0"/>
                            <Button Style="{StaticResource BtnAccent}"
                                    Content="Preview"
                                    Command="{Binding PreviewCommand}"
                                    Width="100"
                                    Margin="0,0,8,0"/>
                            <Button Style="{StaticResource BtnAccent}"
                                    Content="Export WAV"
                                    Command="{Binding ExportCommand}"
                                    Width="100"/>
                        </StackPanel>

                    </Grid>
                </ScrollViewer>
            </Border>

        </DockPanel>
    </Grid>
</Window>
